/**
 * @file example_calibration.ino
 * @brief Example showing sensor calibration sequence.
 */

/*
#include "Micromouse.h"

void setup() {
    robot_init_all();
    
    oled_print(0, 0, "Keep still...");
    oled_update();
    
    calibrate_gyro();
    
    oled_print(0, 10, "Place in cell...");
    oled_update();
    
    delay(3000); // Give user time to place robot
    calibrate_distance_sensors();
    
    oled_print(0, 20, "Done!");
    oled_update();
}

void loop() {
}
*/
