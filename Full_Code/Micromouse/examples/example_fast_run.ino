/**
 * @file example_fast_run.ino
 * @brief Example demonstrating calling fast run trajectory generation.
 */

/*
#include "Micromouse.h"

void setup() {
    robot_init_all();
    
    // Assume maze is solved and optimal path is in memory
    MotionCommand path[50];
    int count = maze_get_fast_path(path);
    
    executor_load_commands(path, count);
    fsm_set_state(STATE_FAST_RUN);
}

void loop() {
    fsm_update();
}
*/
