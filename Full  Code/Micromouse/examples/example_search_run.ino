/**
 * @file example_search_run.ino
 * @brief Example showing how a basic search run might be scripted.
 *
 * This is an example, not meant to be compiled directly into the main project
 * unless you rename it or move it out of the folder.
 */

/*
#include "Micromouse.h" // Hypothetical monolithic header

void setup() {
    robot_init_all();
    fsm_set_state(STATE_SEARCH_RUN);
}

void loop() {
    fsm_update();
}
*/
